#include<stdio.h>
#include<math.h>
int main()
{
	float a,b;
	printf("Enter the two values\n");
	scanf("%f %f",&a,&b);
	printf("The integer multiplication of the two numbers is %d\n",(int)(a*b));
	printf("The floating point multiplication of the two numbers is %f",a*b);
	return 0;
	
}